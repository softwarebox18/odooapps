<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_many2one_field_config_tree" model="ir.ui.view">
        <field name="name">many2one.field.config.tree</field>
        <field name="model">many2one.field.config</field>
        <field name="arch" type="xml">
            <tree string="Many2One Field">
                <field name="model_id"/>
            </tree>
        </field>
    </record>

    <record id="view_many2one_field_config_form" model="ir.ui.view">
        <field name="name">many2one.field.config.form</field>
        <field name="model">many2one.field.config</field>
        <field name="arch" type="xml">
            <form string="Many2One Field Config">
                <group>
                    <field name="model_id" required="1" help="Select model in which you want to add Many2One Field Options Dynamically." placeholder="Select model in which you want to add Many2One Field Options Dynamically."/>
                </group>
                <field name="config_line_ids">
                    <tree editable="bottom">
                        <field name='fields_id' help="Choose fields to add dynamic options" placeholder="Choose fields to add dynamic options" domain="[('model_id', '=', parent.model_id),('ttype', '=', 'many2one')]"/>
                        <field name='is_hide_create_edit' help="Check to hide quick create and edit option."/>
                        <field name='search_limit' help="Limit the number of records in many2one dropdown."/>
                        <field name='quick_search_more_limit' help="Limit the number of records when click on search more... option."/>
                        <field name='is_hide_search_more' help="Check to hide search more... option."/>
                        <field name='can_open' help="Check to hide quick edit option."/>
                    </tree>
                </field>
            </form>
        </field>
    </record>

    <record id="action_many2one_field_config" model="ir.actions.act_window">
        <field name="name">Many2One Field Config</field>
        <field name="res_model">many2one.field.config</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_many2one_field_config_tree"/>
    </record>

    <menuitem id="menu_many2one_field_config" name="Many2One Field Config" groups="base.group_erp_manager"
              action="action_many2one_field_config"/>

</odoo>
